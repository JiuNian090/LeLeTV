# TMDB API Key
# 申请地址: https://www.themoviedb.org/settings/api
TMDB_API_KEY=YOUR_TMDB_API_KEY

# Cloudflare Worker 地址
# 所有邀请码验证和 TMDB 请求都通过 Worker 处理
# ADMINUSER、ADMINKEY、HIDDENKEY 配置在 Worker 端，本地不需要
TMDB_WORKER_URL=https://your-worker.xxx.workers.dev

# 本地服务器端口（可选，默认 8080）
PORT=8080
